---
description: PRD 写作流程（可含红队/预演与用户故事）
argument-hint: "<功能或问题陈述>"
---

# /pm-prd

遵循 **pm-workflow**，启动 `prd`：

```bash
python3 ~/.cursor/skill-os/scripts/flow_cli.py start prd --topic "$ARGUMENTS" --force
```

- 中文 B2B/SaaS 风格 → `write-prd`
- 英文 8 段模板 → `create-prd`

先问偏好，再进入 draft 节点。
