---
description: 查看当前 PM 流程状态与已完成节点
---

# /pm-status

运行并解读：

```bash
python3 ~/.cursor/skill-os/scripts/flow_cli.py status
python3 ~/.cursor/skill-os/scripts/flow_cli.py show
```

用中文说明：当前流程、节点、已完成步骤、下一步、已解析 skill。若无 active flow，提示 `/pm` 或 `/pm-discover` 等。
