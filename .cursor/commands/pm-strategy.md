---
description: 产品战略 Canvas 流程
argument-hint: "<产品或公司>"
---

# /pm-strategy

遵循 **pm-workflow**，启动 `strategy` 流程：

```bash
python3 .cursor/skill-os/scripts/flow_cli.py start strategy --topic "$ARGUMENTS" --force
```

按节点执行：context → product-vision → product-strategy → 输出文档 → handoff。
