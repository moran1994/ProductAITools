---
description: Skill OS — 技能管理、识别与 PM 节点化流程路由（始终生效）
alwaysApply: true
---

# Skill OS 路由规则

本仓库使用 **Skill OS** 管理 Cursor skills，PM 能力按 **流程节点** 执行，禁止把 70+ skill 当零散工具乱点。

## 识别优先级

1. 用户使用 `/pm*`、`/skills` 或提到「流程 / 下一步 / 发现 / 战略 / PRD」→ 读并遵循 `pm-workflow` 或 `skill-os`
2. 多步骤产品工作 → 先选 flow（见 `.cursor/skill-os/flows/index.json`），再按节点 `Read` 对应 skill
3. 单点明确任务（如「只写一段 SQL」）→ catalog 匹配单个 skill 后执行
4. 不确定 → `/pm`（`pm-lifecycle`）让用户选阶段

## 节点引用

- 执行 skill 前必须 `Read` 其 `SKILL.md`
- 回复中标注：`节点 <id>` + `` `skill-id` ``
- 状态机：`.cursor/skill-os/state/active.json`；用 `scripts/flow_cli.py` 前进

## Checkpoint

flow 节点若 `checkpoint: true`，必须停顿确认后再 `next`。

## 清单维护

技能增减后运行：`python3 .cursor/skill-os/scripts/sync_catalog.py`
