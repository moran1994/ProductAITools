---
description: 产品经理AI空间站 — 系统级技能管理与 PM 节点化流程（始终生效）
alwaysApply: true
---

# 产品经理AI空间站 路由规则（系统级）

本机启用 **产品经理AI空间站**（Skill OS），默认路径在用户级，**所有项目**通用：

- Runtime：`~/.cursor/skill-os/`
- Skills：`~/.cursor/skills/`
- Commands：`~/.cursor/commands/`（`/pm*`、`/skills`）

## 识别优先级

1. `/pm*`、`/skills` 或「流程 / 发现 / 战略 / PRD」→ `pm-workflow` 或 `skill-os`
2. 多步骤产品工作 → `python3 ~/.cursor/skill-os/scripts/flow_cli.py` 按节点执行
3. 单点任务 → catalog 匹配单个 skill
4. 不确定 → `/pm`

## 节点引用

- 执行前必须 Read `~/.cursor/skills/<id>/SKILL.md`
- 状态：`~/.cursor/skill-os/state/active.json`
- Checkpoint 必须确认后再 `next`

## 清单维护

`python3 ~/.cursor/skill-os/scripts/sync_catalog.py`
